apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cloud-pro-v3-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: cloud-pro-v3.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: cloud-pro-v3-service
            port:
              number: 80
